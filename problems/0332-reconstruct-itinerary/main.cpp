#include <vector>
#include <string>

class Solution
{
    // Ideas:
    // 1. Euler's walk
    // 2. MIT 6.006 lecture 19 (replicating graph to represent time) and then find the topological order
    // by converting topological sort in a graph to a topological sort in dAG
public:
    std::vector<std::string> findItinerary(const std::vector<std::vector<std::string>> &tickets) {}
};